/* while */

let var sum := 0 in
   while(1) do (
           sum := sum + 1;
           (while (1) do (
                 sum := sum + 2;
                 if sum > 10 then break
           ));
           assert(sum = 11);
           if sum > 12 then break
   );
   assert(sum = 13)
end
