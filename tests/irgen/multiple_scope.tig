let
  var a := 0
  function foo(): int =
    let var b := 1
        function bar():int =
          let var c := 2
              function f():int =
                a - b - c
          in
          assert(f() = -3);
          f()/* f() )pass current fp */
          end
    in
    assert(bar() = -3);
    bar() /* pass current fp */
    end
in
  assert(foo() = -3);
  foo() /* pass current fp */
end
